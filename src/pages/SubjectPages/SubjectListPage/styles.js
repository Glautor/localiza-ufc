import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  headerText: {
    fontSize: 25,
    fontWeight: 'bold',
    marginLeft: 10,
    color: '#363636'
  },
});

export default styles